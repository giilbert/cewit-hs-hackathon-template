services:
  inference:
    build: .
    image: yolo-inference:latest
    runtime: nvidia
    ports:
      - "18001:9001"
    volumes:
      - ./models:/app/models
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
